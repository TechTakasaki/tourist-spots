<div class="cover">
  <div class="cover-inner">
    <div class="cover-contents">
      <h1>また行きたい。あの場所に</h1>
          <% unless logged_in? %>
            <%= link_to '新規の方は会員登録を', signup_path, class: 'btn btn-success btn-lg' %>
            <%= link_to '会員の方はログイン', login_path, class: 'btn btn-success btn-lg' %>
          <% end %>
    </div>
  </div>
</div>